import type { CollectionEntry } from 'astro:content';

/** Data shape rendered by ToolboxPage — the `data` of the single `ai-toolbox` entry. */
export type ToolboxData = CollectionEntry<'ai-toolbox'>['data'];
