{
  "title": {
    "es": "Backend & Fullstack — 9 de septiembre de 2026",
    "en": "Backend & Fullstack — September 9, 2026",
    "fr": "Backend & Fullstack — 9 septembre 2026"
  },
  "description": {
    "es": "99 advisories en un solo día. Un bug en libheif sube por sharp hasta Next.js (CVSS 9.5) y Astro con RCE sin autenticar, multer se cae con un request, y medio ecosistema aparece con complejidad cuadrática.",
    "en": "99 advisories in a single day. One libheif bug climbs through sharp into Next.js (CVSS 9.5) and Astro as unauthenticated RCE, multer dies on one request, and half the ecosystem turns up quadratic.",
    "fr": "99 advisories en un seul jour. Un bug libheif remonte via sharp jusqu'à Next.js (CVSS 9.5) et Astro en RCE non authentifiée, multer tombe sur une requête, et la moitié de l'écosystème s'avère quadratique."
  },
  "pubDate": "2026-09-09",
  "category": "backend-fullstack",
  "highlights": [
    {
      "text": {
        "es": "Next.js: RCE sin auth por AVIF, CVSS 9.5",
        "en": "Next.js: unauthenticated AVIF RCE, CVSS 9.5",
        "fr": "Next.js : RCE AVIF non authentifiée, CVSS 9.5"
      },
      "anchor": "nextjs-avif-rce",
      "icon": "simple-icons:nextdotjs"
    },
    {
      "text": {
        "es": "Next.js: segundo RCE crítico en hosts Windows",
        "en": "Next.js: a second critical RCE on Windows hosts",
        "fr": "Next.js : une seconde RCE critique sur hôtes Windows"
      },
      "anchor": "nextjs-windows-rce",
      "icon": "lucide:shield"
    },
    {
      "text": {
        "es": "sharp 0.35.4: el origen está en libheif",
        "en": "sharp 0.35.4: the origin is in libheif",
        "fr": "sharp 0.35.4 : l'origine est dans libheif"
      },
      "anchor": "sharp-libheif",
      "icon": "lucide:package"
    },
    {
      "text": {
        "es": "Astro 7.2.8 parchea el mismo AVIF",
        "en": "Astro 7.2.8 patches the same AVIF hole",
        "fr": "Astro 7.2.8 corrige la même faille AVIF"
      },
      "anchor": "astro-avif-rce",
      "icon": "simple-icons:astro"
    },
    {
      "text": {
        "es": "multer: un request tira el proceso Node",
        "en": "multer: one request kills the Node process",
        "fr": "multer : une requête tue le processus Node"
      },
      "anchor": "multer-dos",
      "icon": "simple-icons:nodedotjs"
    },
    {
      "text": {
        "es": "Gitea: RCE con solo registrarte",
        "en": "Gitea: RCE from just signing up",
        "fr": "Gitea : RCE en s'inscrivant simplement"
      },
      "anchor": "gitea-diffpatch-rce",
      "icon": "simple-icons:github"
    },
    {
      "text": {
        "es": "El día que el ecosistema resultó cuadrático",
        "en": "The day the ecosystem turned out quadratic",
        "fr": "Le jour où l'écosystème s'est révélé quadratique"
      },
      "anchor": "quadratic-complexity-day",
      "icon": "lucide:layers"
    }
  ],
  "tools": [
    {
      "id": "top-stories",
      "name": "Top Stories",
      "theme": "top-stories",
      "icon_emoji": "🔥",
      "anchor": "nextjs-avif-rce",
      "updates": [
        {
          "id": "nextjs-avif-rce",
          "tag": "breaking",
          "date": "08 Sep 2026",
          "title": {
            "es": "Next.js: RCE sin autenticar en la Image Optimization API vía AVIF (CVSS 9.5)",
            "en": "Next.js: unauthenticated RCE in the Image Optimization API via AVIF (CVSS 9.5)",
            "fr": "Next.js : RCE non authentifiée dans l'Image Optimization API via AVIF (CVSS 9.5)"
          },
          "body_html": {
            "es": "<p><strong>Actualizá ya.</strong> Una vulnerabilidad en <code>libheif</code> —la librería que usa <code>sharp</code>, que a su vez usa Next.js para optimizar imágenes— permite <strong>ejecución remota de código cuando se optimizan archivos AVIF</strong>. Sin autenticación. CVSS 9.5.</p><p>El rango afectado es enorme: <code>&gt;= 10.0.0, &lt; 15.5.24</code> y <code>&gt;= 16.0.0, &lt; 16.3.3</code>. O sea, prácticamente cualquier Next.js de los últimos años con la Image Optimization API expuesta. Parcheado en <strong>15.5.24</strong> y <strong>16.3.3</strong>.</p><p>Mientras el fix se propaga, <strong>Next.js directamente deshabilitó la optimización de AVIF</strong>. Esa decisión te dice todo sobre la gravedad: cuando un framework prefiere romper una feature antes que esperar el parche del upstream, no es un caso teórico.</p>",
            "en": "<p><strong>Update now.</strong> A vulnerability in <code>libheif</code> — the library used by <code>sharp</code>, which Next.js in turn uses for image optimization — allows <strong>remote code execution when AVIF files are optimized</strong>. No authentication required. CVSS 9.5.</p><p>The affected range is enormous: <code>&gt;= 10.0.0, &lt; 15.5.24</code> and <code>&gt;= 16.0.0, &lt; 16.3.3</code>. That is essentially any Next.js from the last several years with the Image Optimization API exposed. Fixed in <strong>15.5.24</strong> and <strong>16.3.3</strong>.</p><p>Until the fix propagates, <strong>Next.js has simply disabled AVIF optimization</strong>. That decision tells you everything about the severity: when a framework would rather break a feature than wait on an upstream patch, this is not theoretical.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-2xp9-vwfh-vxw4", "label": "github.com/advisories" }
        },
        {
          "id": "nextjs-windows-rce",
          "tag": "breaking",
          "date": "08 Sep 2026",
          "title": {
            "es": "Next.js: segundo RCE crítico, sin workaround, en servidores Windows",
            "en": "Next.js: a second critical RCE, no workaround, on Windows servers",
            "fr": "Next.js : seconde RCE critique, sans contournement, sur serveurs Windows"
          },
          "body_html": {
            "es": "<p>CVE-2026-75604, y es <strong>independiente del bug de AVIF</strong>. Aplicaciones que usan Pages y App router <strong>sin Cache Component</strong> pueden llegar a ejecución remota de código cuando el server corre sobre un filesystem Windows. Afecta <code>&gt;= 13.4.0, &lt; 15.5.24</code> y <code>&gt;= 16.0.0, &lt; 16.3.3</code>.</p><p>El advisory es tajante: <strong>«no hay workaround conocido para aplicaciones hosteadas en Windows. Deberías actualizar inmediatamente»</strong>. Sin mitigación parcial, sin config para desactivar la ruta afectada. Solo el upgrade.</p><p>Dos críticos de RCE sin auth en el mismo framework, el mismo día. Si tenés Next.js en producción, esto es lo primero de tu día — y el segundo parche es el mismo release que el primero, así que un solo upgrade te cubre los dos.</p>",
            "en": "<p>CVE-2026-75604, and it is <strong>independent of the AVIF bug</strong>. Applications using Pages and App router <strong>without Cache Component</strong> can reach remote code execution when the server runs on a Windows filesystem. Affects <code>&gt;= 13.4.0, &lt; 15.5.24</code> and <code>&gt;= 16.0.0, &lt; 16.3.3</code>.</p><p>The advisory is blunt: <strong>«there is no known workaround for affected windows-hosted applications. You should upgrade immediately»</strong>. No partial mitigation, no config flag to disable the affected path. Only the upgrade.</p><p>Two unauthenticated RCE criticals in the same framework on the same day. If you run Next.js in production this is the first thing in your day — and the second patch ships in the same release as the first, so one upgrade covers both.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-p293-qw3h-jr36", "label": "github.com/advisories" }
        },
        {
          "id": "sharp-libheif",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "sharp 0.35.4: el origen real de la cadena está en libheif",
            "en": "sharp 0.35.4: the real origin of the chain is libheif",
            "fr": "sharp 0.35.4 : l'origine réelle de la chaîne est libheif"
          },
          "body_html": {
            "es": "<p>Acá está la raíz de todo lo de arriba. Se encontraron y corrigieron <strong>varias vulnerabilidades en la dependencia upstream libheif, dos de ellas críticas en CVSSv3</strong>, que pueden llevar a RCE en Linux con glibc bajo ciertas condiciones.</p><p>El detalle de scoring es instructivo: el vector de ataque original dice «network», pero <strong>sharp no expone ninguna funcionalidad de red</strong>, así que lo bajaron a «high» en CVSSv4 — con la advertencia explícita de mirar el <em>Subsequent System Impact</em>. Traducción: el número es más bajo porque sharp no es el que te expone a internet; <strong>el framework que lo envuelve sí</strong>. Y por eso Next.js puntúa 9.5 y sharp 8.9 por el mismo bug.</p><p>Actualizá a <code>0.35.4</code>, que trae libheif 1.23.2. Si usás un libheif instalado globalmente en vez de los binarios preconstruidos, asegurate de tener <strong>1.23.2</strong> a nivel sistema — el bump de npm no te cubre.</p>",
            "en": "<p>Here is the root of everything above. Several vulnerabilities were found and fixed in the upstream libheif dependency, <strong>two rated Critical under CVSSv3</strong>, which can lead to RCE on glibc-based Linux under certain conditions.</p><p>The scoring detail is instructive: the original attack vector says «network», but <strong>sharp exposes no networking features</strong>, so it was down-rated to High under CVSSv4 — with an explicit note to mind the <em>Subsequent System Impact</em>. Translation: the number is lower because sharp is not what exposes you to the internet; <strong>the framework wrapping it is</strong>. Which is exactly why Next.js scores 9.5 and sharp 8.9 for the same bug.</p><p>Upgrade to <code>0.35.4</code>, which ships libheif 1.23.2. If you use a globally installed libheif rather than the prebuilt binaries, make sure the system has <strong>1.23.2</strong> — the npm bump will not cover you.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-rgj7-g3m4-5g8c", "label": "github.com/advisories" }
        },
        {
          "id": "astro-avif-rce",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Astro 7.2.8: el mismo agujero de AVIF por el mismo camino",
            "en": "Astro 7.2.8: the same AVIF hole through the same path",
            "fr": "Astro 7.2.8 : la même faille AVIF par le même chemin"
          },
          "body_html": {
            "es": "<p>Crítico, y es literalmente el mismo bug: la vulnerabilidad de <code>libheif</code>, usada por el <strong>servicio de imágenes Sharp que Astro trae por defecto</strong>, lleva a RCE cuando se optimiza un AVIF malicioso. Estás afectado si un atacante puede lograr que Astro procese un AVIF que no controlás.</p><p>Fix en <strong>Astro 7.2.8</strong>, que <strong>requiere Sharp 0.35.4</strong>. Prestá atención a eso: no alcanza con bumpear Astro si tu lockfile te deja sharp viejo — verificá la versión resuelta de verdad.</p>",
            "en": "<p>Critical, and it is literally the same bug: the <code>libheif</code> vulnerability, reached through the <strong>Sharp image service Astro ships by default</strong>, leads to RCE when a malicious AVIF is optimized. You are affected if an attacker can get Astro to process an AVIF you do not control.</p><p>Fixed in <strong>Astro 7.2.8</strong>, which <strong>requires Sharp 0.35.4</strong>. Pay attention to that: bumping Astro is not enough if your lockfile pins an older sharp — verify the actually resolved version.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-26w7-cxv4-gfx2", "label": "github.com/advisories" }
        }
      ]
    },
    {
      "id": "security",
      "name": "Security",
      "theme": "security",
      "icon_emoji": "🔒",
      "anchor": "multer-dos",
      "updates": [
        {
          "id": "multer-dos",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "multer: dos nombres de campo y te matan el proceso Node",
            "en": "multer: two field names and your Node process is dead",
            "fr": "multer : deux noms de champ et le processus Node meurt"
          },
          "body_html": {
            "es": "<p>Cuatro advisories contra multer el mismo día, y el peor es brutal por lo barato: <strong>un atacante remoto sin autenticar crashea el proceso Node con un solo request <code>multipart/form-data</code></strong>. Dos nombres de campo de texto crafteados provocan un <code>RangeError: Invalid array length</code> sin capturar dentro del parseo de campos — y <strong>no se rutea al error handler de la aplicación</strong>, termina el proceso. Todas las apps que usan multer están afectadas. <strong>Sin workaround</strong>, upgrade a <code>2.3.0</code>.</p><p>Los otros tres: DoS por leak de file descriptors en uploads abortados, DoS por índice de array sobredimensionado en nombres de campo, y bypass del límite de tamaño de archivo por una race en <code>fileFilter</code> asíncrono.</p><p>Si tenés un endpoint de upload en Express expuesto a internet, este es tu segundo item del día después de Next.js.</p>",
            "en": "<p>Four advisories against multer on the same day, and the worst is brutal for how cheap it is: <strong>a remote unauthenticated attacker crashes the Node process with a single <code>multipart/form-data</code> request</strong>. Two crafted text field names trigger an uncaught <code>RangeError: Invalid array length</code> inside field parsing — and <strong>it is not routed to the application error handler</strong>, it terminates the process. Every app using multer is affected. <strong>No workaround</strong>, upgrade to <code>2.3.0</code>.</p><p>The other three: DoS via file descriptor leak on aborted uploads, DoS via oversized array index in field names, and a file size limit bypass through an async <code>fileFilter</code> race condition.</p><p>If you have an Express upload endpoint facing the internet, this is your second item of the day after Next.js.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-wc9g-mqfw-jrwm", "label": "github.com/advisories" }
        },
        {
          "id": "nodemailer-parser",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Nodemailer: el parser de direcciones es cuadrático y el de dominios no sigue el estándar",
            "en": "Nodemailer: the address parser is quadratic and the domain parser ignores the standard",
            "fr": "Nodemailer : le parser d'adresses est quadratique et celui de domaines ignore le standard"
          },
          "body_html": {
            "es": "<p>Cuatro advisories. El de severidad alta: <code>lib/addressparser/index.js</code> parsea listas de direcciones separadas por coma en <strong>tiempo cuadrático O(n²)</strong>. Un solo string crafteado en <code>To</code>, <code>Cc</code>, <code>Bcc</code>, <code>From</code> o <code>Reply-To</code> consume CPU proporcional al <strong>cuadrado</strong> de su longitud y <strong>bloquea el event loop de Node entero</strong>, negando servicio a todas las demás requests del proceso.</p><p>El más sutil, y el que me parece más peligroso: Nodemailer resuelve dominios internacionales (IDN) a una etiqueta Punycode <strong>distinta de la que resuelve cualquier parser conforme a UTS-46</strong> — browsers, el WHATWG URL Standard, <code>url.domainToASCII</code> de Node, <code>idna</code> de Python. Usa el codec RFC-3492 crudo sin mapping UTS-46. ¿Consecuencia? <strong>Un dominio que tu validador mapea a un dominio confiable, Nodemailer lo entrega a otro</strong>. Tu allow-list dice que sí y el mail sale para el atacante.</p><p>Sumale el bypass de validación de dominio por mal parseo de comentarios RFC 5322 y el de <code>resolveContent()</code> salteando <code>disableFileAccess</code>/<code>disableUrlAccess</code>. Todo a <code>9.1.0</code>.</p>",
            "en": "<p>Four advisories. The high-severity one: <code>lib/addressparser/index.js</code> parses comma-separated address lists in <strong>quadratic O(n²) time</strong>. A single crafted string in <code>To</code>, <code>Cc</code>, <code>Bcc</code>, <code>From</code>, or <code>Reply-To</code> burns CPU proportional to the <strong>square</strong> of its length and <strong>blocks Node's entire event loop</strong>, denying service to every other request in the process.</p><p>The subtler one, and to me the more dangerous: Nodemailer resolves international (IDN) domains to a Punycode label <strong>different from every UTS-46-conformant parser</strong> — browsers, the WHATWG URL Standard, Node's <code>url.domainToASCII</code>, Python's <code>idna</code>. It uses the raw RFC-3492 codec with no UTS-46 mapping. The consequence? <strong>A domain your validator maps to a trusted one, Nodemailer delivers somewhere else</strong>. Your allow-list says yes and the mail goes to the attacker.</p><p>Add the domain validation bypass via RFC 5322 comment mis-parsing, and <code>resolveContent()</code> bypassing <code>disableFileAccess</code>/<code>disableUrlAccess</code>. All fixed in <code>9.1.0</code>.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-wmmp-3585-3rmp", "label": "github.com/advisories" }
        },
        {
          "id": "hono-tossg-incomplete",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Hono: el fix de CVE-2026-39408 no cubría todas las secuencias de traversal",
            "en": "Hono: the CVE-2026-39408 fix did not cover every traversal sequence",
            "fr": "Hono : le correctif de CVE-2026-39408 ne couvrait pas toutes les séquences"
          },
          "body_html": {
            "es": "<p>Tres advisories. El principal es un <strong>fix incompleto</strong>: <code>toSSG()</code> todavía puede escribir archivos fuera del directorio de salida cuando un parámetro de ruta contiene segmentos <code>..</code> consecutivos. La causa es elegante y didáctica: la verificación de que el path resultante queda dentro del output <strong>normaliza el path con la misma rutina que lo construyó</strong>. Si la rutina no normaliza del todo, el chequeo hereda exactamente el mismo punto ciego.</p><p>Los otros dos: anidamiento sin límite de dot-notation en <code>parseBody()</code> causando agotamiento de memoria, y el query parser leyendo parámetros <strong>después del fragmento de la URL</strong>, lo que genera diferenciales de interpretación entre tu app, el proxy y la cache key. Ese último es material clásico de cache poisoning. Fix en <code>4.13.5</code>.</p>",
            "en": "<p>Three advisories. The main one is an <strong>incomplete fix</strong>: <code>toSSG()</code> can still write files outside the output directory when a route parameter contains consecutive <code>..</code> segments. The cause is elegant and instructive: the check that the resulting path stays inside the output <strong>normalizes the path with the same routine that built it</strong>. If that routine does not fully normalize, the check inherits exactly the same blind spot.</p><p>The other two: unbounded dot-notation nesting in <code>parseBody()</code> causing memory exhaustion, and the query parser reading parameters <strong>after the URL fragment</strong>, producing interpretation differentials between your app, the proxy, and the cache key. That last one is classic cache poisoning material. Fixed in <code>4.13.5</code>.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-gqvv-2mrq-wpjv", "label": "github.com/advisories" }
        },
        {
          "id": "vitest-mocker-traversal",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Vitest: lectura arbitraria de archivos vía redirect mock del dev server",
            "en": "Vitest: arbitrary file read via the dev server's redirect mock",
            "fr": "Vitest : lecture arbitraire de fichiers via le redirect mock du dev server"
          },
          "body_html": {
            "es": "<p><code>@vitest/mocker</code> registra el path destino de un redirect mock <strong>sin validarlo contra la allowlist de file-serving del dev server</strong>. Quien pueda alcanzar el WebSocket del dev server registra un mock apuntando fuera del root del proyecto, y cuando se pide ese módulo el hook <code>load</code> devuelve <code>readFile(&lt;path del atacante&gt;)</code> como código fuente del módulo. Explotable <strong>sin autenticación</strong> a través del <code>mockerPlugin</code> público.</p><p>Afecta desde <code>2.1.0</code> hasta <code>4.1.11</code>, y también la línea 5 beta hasta <code>5.0.0-rc.2</code>. Otro caso más de tooling de desarrollo escuchando más de lo que creías — el mismo tema que venimos viendo toda la semana.</p>",
            "en": "<p><code>@vitest/mocker</code> registers a redirect mock's target path <strong>without validating it against the dev server's file-serving allowlist</strong>. Anyone who can reach the dev server's WebSocket registers a mock pointing outside the project root, and when that module is requested the <code>load</code> hook returns <code>readFile(&lt;attacker path&gt;)</code> as the module source. Exploitable <strong>without authentication</strong> through the public <code>mockerPlugin</code>.</p><p>Affects <code>2.1.0</code> through <code>4.1.11</code>, and the 5 beta line up to <code>5.0.0-rc.2</code>. One more case of development tooling listening more widely than you assumed — the same theme we have been seeing all week.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-82fw-gwwq-j7x9", "label": "github.com/advisories" }
        },
        {
          "id": "xmldom-quadratic",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "xmldom: catorce advisories, y la deduplicación de atributos es O(M²)",
            "en": "xmldom: fourteen advisories, and attribute deduplication is O(M²)",
            "fr": "xmldom : quatorze advisories, et la déduplication d'attributs est en O(M²)"
          },
          "body_html": {
            "es": "<p>Catorce advisories contra <code>@xmldom/xmldom</code> en un día, la mayoría en 8.7. El más representativo: construye la colección de atributos insertando de a uno en un <code>NamedNodeMap</code>, y <strong>cada inserción hace un scan lineal de todos los atributos ya insertados</strong> para aplicar la regla de unicidad del DOM. Parsear un elemento con M atributos cuesta <code>1 + 2 + … + M = O(M²)</code>.</p><p>Lo que lo hace peor: <strong>el payload es XML perfectamente bien formado</strong>. No hay nada malformado que detectar, es «un elemento con muchos atributos». Ninguna validación de esquema te salva.</p><p>El resto del paquete es un desfile de bypasses de <code>requireWellFormed</code> —por terminadores de línea embebidos en nombres de elemento, atributo, DocType y publicId/systemId, más inyección vía <code>createElement()</code> y <code>setAttribute()</code>— y varios ReDoS más. Parcheado en <code>0.8.15</code> y <code>0.9.12</code>. El paquete legacy <code>xmldom</code> (&lt;= 0.6.0) <strong>no tiene fix</strong>: migrá.</p>",
            "en": "<p>Fourteen advisories against <code>@xmldom/xmldom</code> in one day, most at 8.7. The most representative: it builds each element's attribute collection by inserting one at a time into a <code>NamedNodeMap</code>, and <strong>every insertion performs a linear scan of all already-inserted attributes</strong> to enforce the DOM uniqueness rule. Parsing an element with M attributes costs <code>1 + 2 + … + M = O(M²)</code>.</p><p>What makes it worse: <strong>the payload is perfectly well-formed XML</strong>. There is nothing malformed to detect — it is «one element with many attributes». No schema validation saves you.</p><p>The rest of the batch is a parade of <code>requireWellFormed</code> bypasses — via embedded line terminators in element, attribute, DocType, and publicId/systemId names, plus injection through <code>createElement()</code> and <code>setAttribute()</code> — and several more ReDoS. Fixed in <code>0.8.15</code> and <code>0.9.12</code>. The legacy <code>xmldom</code> package (&lt;= 0.6.0) has <strong>no fix</strong>: migrate.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-8344-3jmq-59r6", "label": "github.com/advisories" }
        }
      ]
    },
    {
      "id": "cloud",
      "name": "Cloud & DevOps",
      "theme": "cloud",
      "icon_emoji": "☁️",
      "anchor": "gitea-diffpatch-rce",
      "updates": [
        {
          "id": "gitea-diffpatch-rce",
          "tag": "breaking",
          "date": "08 Sep 2026",
          "title": {
            "es": "Gitea: RCE instalando un git hook desde el endpoint diffpatch",
            "en": "Gitea: RCE by installing a git hook through the diffpatch endpoint",
            "fr": "Gitea : RCE en installant un git hook via l'endpoint diffpatch"
          },
          "body_html": {
            "es": "<p>Crítico. El endpoint <code>diffpatch</code> se puede abusar para <strong>instalar y ejecutar un git hook desde contenido controlado por el repositorio</strong>. Un atacante con acceso de escritura ordinario ejecuta comandos de shell arbitrarios <strong>como el usuario del sistema operativo bajo el que corre Gitea</strong>.</p><p>Y acá está la parte que convierte esto en emergencia: <strong>con el registro abierto por defecto, un visitante sin autenticar consigue el acceso de escritura necesario simplemente registrando una cuenta y creando un repositorio</strong>. O sea, de anónimo a RCE en dos pasos triviales. Parcheado en <code>1.27.1</code>.</p><p>Si tenés un Gitea self-hosted con registro abierto, cerralo o actualizá ahora mismo.</p>",
            "en": "<p>Critical. The <code>diffpatch</code> endpoint can be abused to <strong>install and execute a git hook from repository-controlled content</strong>. An attacker with ordinary write access runs arbitrary shell commands <strong>as the OS user Gitea runs under</strong>.</p><p>And here is what turns this into an emergency: <strong>with default open registration, an unauthenticated visitor obtains the required write access by simply registering an account and creating a repository</strong>. Anonymous to RCE in two trivial steps. Fixed in <code>1.27.1</code>.</p><p>If you run self-hosted Gitea with open registration, close it or upgrade right now.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-rcr6-4jqh-j84m", "label": "github.com/advisories" }
        },
        {
          "id": "netty-sni-bypass",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Netty: un ClientHello fragmentado saltea tu mTLS por SNI (CVSS 9.1)",
            "en": "Netty: a fragmented ClientHello bypasses your SNI-based mTLS (CVSS 9.1)",
            "fr": "Netty : un ClientHello fragmenté contourne votre mTLS par SNI (CVSS 9.1)"
          },
          "body_html": {
            "es": "<p>Crítico, 9.1. Un ClientHello de TLS cuyo header de handshake se reparte entre varios records hace que Netty <strong>caiga silenciosamente al <code>SslContext</code> por defecto</strong>. Donde la selección por SNI es la única barrera de mTLS, un atacante sin autenticar <strong>saltea el requisito de mTLS de la ruta</strong>.</p><p>La causa es de manual: en <code>SslClientHelloHandler#decode</code>, el guard que debería esperar los 4 bytes del header de handshake <strong>chequea el offset equivocado — ignora los 5 bytes del record header que lo preceden</strong> — así que nunca dispara. Un off-by-five en el parseo de un protocolo, y se te cae la autenticación mutua.</p><p>Parcheado en <code>4.2.17.Final</code> y <code>4.1.137.Final</code>. Viene con un segundo advisory por reensamblado cuadrático pre-handshake en el parseo de SNI por defecto.</p>",
            "en": "<p>Critical, 9.1. A TLS ClientHello whose handshake header spans multiple records makes Netty <strong>silently fall back to the default <code>SslContext</code></strong>. Where per-SNI selection is the sole mTLS gate, an unauthenticated attacker <strong>bypasses the route's mTLS requirement</strong>.</p><p>The cause is textbook: in <code>SslClientHelloHandler#decode</code>, the guard that should wait for the 4-byte handshake header <strong>checks the wrong offset — it ignores the 5-byte record header preceding it</strong> — and therefore never fires. An off-by-five in protocol parsing, and your mutual authentication is gone.</p><p>Fixed in <code>4.2.17.Final</code> and <code>4.1.137.Final</code>. It ships alongside a second advisory for quadratic pre-handshake reassembly in default SNI parsing.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-c4c3-7fpv-j4q5", "label": "github.com/advisories" }
        },
        {
          "id": "gitpython-config-rce",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "GitPython: un valor dormido en git-config se despierta como directiva viva (CVSS 9.3)",
            "en": "GitPython: a dormant git-config value wakes up as a live directive (CVSS 9.3)",
            "fr": "GitPython : une valeur dormante de git-config se réveille en directive vivante"
          },
          "body_html": {
            "es": "<p>Crítico, 9.3, y el mecanismo es de los más lindos que vi en el año. Valores multilínea <strong>dormidos</strong> en un <code>git-config</code> se <strong>corrompen convirtiéndose en directivas inyectadas vivas</strong> —por ejemplo <code>core.hooksPath</code>— <strong>en cualquier escritura no relacionada del <code>GitConfigParser</code></strong>. O sea: el valor era inerte, vos escribís otra cosa completamente distinta en el config, y el round trip de lectura-escritura lo activa. De ahí a RCE.</p><p>Es un <strong>read-then-corrupt-on-rewrite</strong>, no una inyección por argumento de un setter. Por eso se escapa de cualquier revisión que mire solo las llamadas a la API. Parcheado en <code>3.1.59</code>.</p><p>Vienen cuatro advisories más de GitPython el mismo día: <code>clone_from()</code> omitiendo <code>--separate-git-dir</code> de las opciones inseguras, denylist incompleta que permite lectura arbitraria vía <code>Repo.blame()</code>, y disclosure de archivos por directiva <code>[include]</code> en un <code>.gitmodules</code> no confiable.</p>",
            "en": "<p>Critical, 9.3, and the mechanism is one of the prettiest I have seen this year. <strong>Dormant</strong> multi-line values in a <code>git-config</code> are <strong>corrupted into live injected directives</strong> — for example <code>core.hooksPath</code> — <strong>on any unrelated <code>GitConfigParser</code> write</strong>. The value was inert, you write something completely different to the config, and the read-write round trip activates it. From there, RCE.</p><p>It is a <strong>read-then-corrupt-on-rewrite</strong>, not an injection through a setter argument. Which is why it slips past any review that only inspects API calls. Fixed in <code>3.1.59</code>.</p><p>Four more GitPython advisories landed the same day: <code>clone_from()</code> omitting <code>--separate-git-dir</code> from unsafe options, an incomplete denylist enabling arbitrary file read via <code>Repo.blame()</code>, and file disclosure through an <code>[include]</code> directive in an untrusted <code>.gitmodules</code>.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-284h-m62q-gf8w", "label": "github.com/advisories" }
        }
      ]
    },
    {
      "id": "architecture",
      "name": "Architecture & Best Practices",
      "theme": "architecture",
      "icon_emoji": "🏗️",
      "anchor": "quadratic-complexity-day",
      "updates": [
        {
          "id": "quadratic-complexity-day",
          "tag": "notable",
          "date": "08 Sep 2026",
          "title": {
            "es": "El día que medio ecosistema resultó cuadrático",
            "en": "The day half the ecosystem turned out to be quadratic",
            "fr": "Le jour où la moitié de l'écosystème s'est révélée quadratique"
          },
          "body_html": {
            "es": "<p>Contá los del 8 de septiembre: xmldom (dedup de atributos O(M²), ReDoS de PI, parseo cuadrático en recovery, memoria cuadrática), Nodemailer (addressparser O(n²)), Tiptap (ReDoS cuadrático en parseo de atributos Markdown), Netty (reensamblado cuadrático pre-handshake), HTTPX2 (buffering cuadrático de SSE), Colord (rechazo lento de strings malformados), NLTK (tres ReDoS distintos), y este de LiquidJS: el filtro <code>join</code> permite <strong>saltear el <code>memoryLimit</code> y crashear el proceso</strong>.</p><p>No es coincidencia, es una <strong>clase de bug que estamos sistemáticamente sin testear</strong>. Y tiene una firma reconocible: el input es <em>válido</em>. Bien formado, dentro de los límites de tamaño, pasa cualquier validación de esquema. Lo que lo hace tóxico no es el contenido sino la <strong>forma</strong> — profundidad de anidamiento, cantidad de atributos, cantidad de elementos en una lista.</p><p>La conclusión práctica: <strong>tus límites cuentan bytes, y el costo real casi nunca está en los bytes</strong>. Un límite de tamaño de request no te protege de un documento chiquito y muy anidado. Cuando escribas un parser, medí el costo en función de la <em>forma</em> del input, no de su peso — y poné un límite explícito sobre esa dimensión.</p>",
            "en": "<p>Count the September 8 batch: xmldom (O(M²) attribute dedup, PI ReDoS, quadratic recovery parsing, quadratic memory), Nodemailer (O(n²) addressparser), Tiptap (quadratic ReDoS in Markdown attribute parsing), Netty (quadratic pre-handshake reassembly), HTTPX2 (quadratic SSE buffering), Colord (slow rejection of malformed strings), NLTK (three separate ReDoS), and this LiquidJS one: the <code>join</code> filter lets template authors <strong>bypass <code>memoryLimit</code> and crash the process</strong>.</p><p>This is not coincidence, it is a <strong>bug class we are systematically failing to test</strong>. And it has a recognizable signature: the input is <em>valid</em>. Well-formed, within size limits, passes any schema validation. What makes it toxic is not the content but the <strong>shape</strong> — nesting depth, attribute count, number of elements in a list.</p><p>The practical conclusion: <strong>your limits count bytes, and the real cost is almost never in the bytes</strong>. A request size limit does not protect you from a small, deeply nested document. When you write a parser, measure cost as a function of the input's <em>shape</em>, not its weight — and put an explicit bound on that dimension.</p>"
          },
          "source": { "url": "https://github.com/advisories/GHSA-4r6h-5v86-94p3", "label": "github.com/advisories" }
        }
      ]
    },
    {
      "id": "runtimes",
      "name": "Backend TypeScript & Runtimes",
      "theme": "runtimes",
      "icon_emoji": "⚡",
      "anchor": "node-24-21-0",
      "updates": [
        {
          "id": "node-24-21-0",
          "tag": "release",
          "date": "08 Sep 2026",
          "title": {
            "es": "Node.js 24.21.0: OpenSSL 3.5.8, certificados raíz nuevos y BlockList más rápido",
            "en": "Node.js 24.21.0: OpenSSL 3.5.8, fresh root certificates, and a faster BlockList",
            "fr": "Node.js 24.21.0 : OpenSSL 3.5.8, nouveaux certificats racine et BlockList plus rapide"
          },
          "body_html": {
            "es": "<p>Release cargado de criptografía: <strong>OpenSSL sube a 3.5.8</strong>, los certificados raíz se actualizan a NSS 3.126, y Undici pasa a 7.29.1. Como minor nuevo, <strong>soporte para cargar claves privadas a través de STORE loaders</strong> de OpenSSL — útil si tenés las claves en un HSM o en un provider externo en vez de en el filesystem.</p><p>Del lado de performance: mejor implementación de histogramas y <strong><code>net.BlockList</code> más rápido</strong>. Si usás BlockList para filtrado de IPs en el hot path, ese es un upgrade gratis.</p>",
            "en": "<p>A crypto-heavy release: <strong>OpenSSL moves to 3.5.8</strong>, root certificates update to NSS 3.126, and Undici goes to 7.29.1. New as a minor, <strong>support for loading private keys through OpenSSL STORE loaders</strong> — useful when your keys live in an HSM or an external provider rather than the filesystem.</p><p>On performance: an improved histogram implementation and a <strong>faster <code>net.BlockList</code></strong>. If you use BlockList for IP filtering on the hot path, that is a free upgrade.</p>"
          },
          "source": { "url": "https://github.com/nodejs/node/releases/tag/v24.21.0", "label": "github.com/nodejs" }
        },
        {
          "id": "workerd-20260909",
          "tag": "release",
          "date": "09 Sep 2026",
          "title": {
            "es": "workerd v1.20260909.1 sostiene la cadencia diaria",
            "en": "workerd v1.20260909.1 holds the daily cadence",
            "fr": "workerd v1.20260909.1 tient la cadence quotidienne"
          },
          "body_html": {
            "es": "<p>Otro release diario del runtime de Workers. En un día donde el resto del ecosistema publicó 99 advisories, la cadencia predecible de Cloudflare es casi un descanso. Revisá el changelog acumulado si trackeás compatibility flags.</p>",
            "en": "<p>Another daily Workers runtime release. On a day when the rest of the ecosystem published 99 advisories, Cloudflare's predictable cadence is almost a relief. Read the accumulated changelog if you track compatibility flags.</p>"
          },
          "source": { "url": "https://github.com/cloudflare/workerd/releases/tag/v1.20260909.1", "label": "github.com/cloudflare" }
        }
      ]
    },
    {
      "id": "fullstack",
      "name": "Fullstack",
      "theme": "fullstack",
      "icon_emoji": "🔗",
      "anchor": "astro-732-mdx-xss",
      "updates": [
        {
          "id": "astro-732-mdx-xss",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Astro 7.3.2: valores dinámicos en script y style de MDX ya se escapan",
            "en": "Astro 7.3.2: dynamic values in MDX script and style are now escaped",
            "fr": "Astro 7.3.2 : les valeurs dynamiques dans script et style MDX sont échappées"
          },
          "body_html": {
            "es": "<p>Además del parche de AVIF, Astro corrige el renderizado de <code>&lt;script&gt;</code> y <code>&lt;style&gt;</code> en MDX: ahora <strong>solo el contenido literal se trata como markup confiable</strong> (incluido el que inyectan plugins de remark/rehype). Un valor dinámico pasado como child —<code>&lt;script&gt;{value}&lt;/script&gt;</code>— <strong>se escapa como el contenido de cualquier otro elemento</strong> en vez de renderizarse crudo. Si querés el comportamiento anterior, ahora hay que optar explícitamente con <code>set:html</code>.</p><p>Esa es la decisión correcta: <strong>el default pasa a ser seguro y la excepción pasa a ser explícita</strong>. Ojo si tenías contenido dinámico legítimo ahí — te va a cambiar el render y tenés que marcarlo a mano.</p>",
            "en": "<p>Alongside the AVIF patch, Astro fixes <code>&lt;script&gt;</code> and <code>&lt;style&gt;</code> rendering in MDX: <strong>only literal content is now treated as trusted markup</strong> (including content injected by remark/rehype plugins). A dynamic value passed as a child — <code>&lt;script&gt;{value}&lt;/script&gt;</code> — is <strong>escaped like any other element's content</strong> instead of rendered raw. To get the old behavior you must now opt in explicitly with <code>set:html</code>.</p><p>That is the right call: <strong>the default becomes safe and the exception becomes explicit</strong>. Watch out if you had legitimate dynamic content there — your render will change and you have to mark it by hand.</p>"
          },
          "source": { "url": "https://github.com/withastro/astro/releases/tag/astro%407.3.2", "label": "github.com/withastro" }
        },
        {
          "id": "pnpm-12-4-0",
          "tag": "release",
          "date": "08 Sep 2026",
          "title": {
            "es": "pnpm 12.4.0 sigue el ritmo de la línea en Rust",
            "en": "pnpm 12.4.0 keeps the Rust line moving",
            "fr": "pnpm 12.4.0 maintient le rythme de la ligne Rust"
          },
          "body_html": {
            "es": "<p>Nuevo minor de la línea 12 nativa, apenas días después de 12.3.x. El ritmo de releases desde el rewrite en Rust es notablemente más alto — bueno para los fixes, pero mantené el ojo en los breaking changes acumulados si venís de la línea 11.</p>",
            "en": "<p>A new minor on the native 12 line, days after 12.3.x. The release rhythm since the Rust rewrite is noticeably higher — good for fixes, but keep an eye on accumulated breaking changes if you are coming from the 11 line.</p>"
          },
          "source": { "url": "https://github.com/pnpm/pnpm/releases/tag/v12.4.0", "label": "github.com/pnpm" }
        }
      ]
    }
  ],
  "notable_trends": [
    {
      "es": "La cadena del día es una clase magistral sobre dependencias transitivas: <strong>un bug en libheif —C, upstream, que probablemente ni sabías que estaba en tu árbol— sube por sharp y sale como RCE sin autenticar con CVSS 9.5 en Next.js y como crítico en Astro</strong>. Mirá cómo cambia el score en el camino: 8.9 en sharp, 9.5 en Next.js. Es el mismo bug; lo que cambia es <strong>cuánta superficie de internet le pone encima cada capa</strong>. Tu análisis de riesgo de dependencias no puede parar en el <code>package.json</code> directo.",
      "en": "The day's chain is a masterclass in transitive dependencies: <strong>a bug in libheif — C, upstream, probably something you did not know was in your tree — climbs through sharp and comes out as unauthenticated RCE at CVSS 9.5 in Next.js and critical in Astro</strong>. Watch the score change along the way: 8.9 at sharp, 9.5 at Next.js. Same bug; what changes is <strong>how much internet surface each layer puts on top of it</strong>. Your dependency risk analysis cannot stop at the direct <code>package.json</code>.",
      "fr": "La chaîne du jour est un cours magistral sur les dépendances transitives : <strong>un bug dans libheif — du C, en amont, probablement inconnu dans votre arbre — remonte via sharp et ressort en RCE non authentifiée à CVSS 9.5 dans Next.js et en critique dans Astro</strong>. Observez le score changer en chemin : 8,9 chez sharp, 9,5 chez Next.js. Même bug ; ce qui change, c'est <strong>la surface internet que chaque couche ajoute</strong>. Votre analyse de risque ne peut s'arrêter au <code>package.json</code> direct."
    },
    {
      "es": "99 advisories en un solo día, con paquetes enteros cayendo en bloque: xmldom 14, NLTK 16, GitPython 5, HTTPX2 5, Nodemailer 4, multer 4, Hono 3. Ese patrón no es «se rompió todo el mismo martes» — es <strong>auditoría sistemática de un paquete a la vez</strong>, muy probablemente asistida por herramientas. La consecuencia para vos es práctica: <strong>cuando veas un advisory de una librería que usás, buscá los hermanos del mismo día antes de parchear</strong>. Actualizar por el CVE que te llegó y dejar los otros tres es peor que no enterarte, porque te da la sensación de estar cubierto.",
      "en": "99 advisories in a single day, with entire packages falling in batches: xmldom 14, NLTK 16, GitPython 5, HTTPX2 5, Nodemailer 4, multer 4, Hono 3. That pattern is not «everything broke on the same Tuesday» — it is <strong>systematic auditing one package at a time</strong>, very likely tool-assisted. The practical consequence: <strong>when you see an advisory for a library you use, look for its same-day siblings before patching</strong>. Upgrading for the CVE that reached you and leaving the other three is worse than not knowing, because it leaves you feeling covered.",
      "fr": "99 advisories en un seul jour, avec des paquets entiers tombant par lots : xmldom 14, NLTK 16, GitPython 5, HTTPX2 5, Nodemailer 4, multer 4, Hono 3. Ce motif n'est pas «tout a cassé le même mardi» — c'est un <strong>audit systématique, un paquet à la fois</strong>, très probablement assisté par outils. Conséquence pratique : <strong>quand vous voyez un advisory pour une librairie que vous utilisez, cherchez ses frères du même jour avant de patcher</strong>. Corriger le CVE qui vous est parvenu en laissant les trois autres est pire que l'ignorance, car cela donne le sentiment d'être couvert."
    },
    {
      "es": "Tres bugs del día comparten la misma raíz conceptual: <strong>una verificación que hereda el defecto de lo que verifica</strong>. En Hono, el chequeo de que <code>toSSG()</code> no escribe fuera del output normaliza el path <em>con la misma rutina que lo construyó</em>. En Netty, el guard que espera el header de handshake mira el offset equivocado y nunca dispara. En GitPython, el parser de config lee un valor inerte y lo reescribe activado. En los tres, <strong>el control existía y estaba escrito con la misma lógica que el error que debía atrapar</strong>. Si tu validación comparte código con la construcción, no es validación — es la misma afirmación dicha dos veces.",
      "en": "Three of the day's bugs share one conceptual root: <strong>a check that inherits the defect of what it checks</strong>. In Hono, the check that <code>toSSG()</code> does not write outside the output normalizes the path <em>with the same routine that built it</em>. In Netty, the guard waiting for the handshake header looks at the wrong offset and never fires. In GitPython, the config parser reads an inert value and writes it back activated. In all three, <strong>the control existed and was written with the same logic as the error it was meant to catch</strong>. If your validation shares code with your construction, it is not validation — it is the same assertion stated twice.",
      "fr": "Trois bugs du jour partagent une racine conceptuelle : <strong>un contrôle qui hérite du défaut de ce qu'il contrôle</strong>. Dans Hono, la vérification que <code>toSSG()</code> n'écrit pas hors de l'output normalise le chemin <em>avec la routine qui l'a construit</em>. Dans Netty, le guard attendant l'en-tête de handshake regarde le mauvais offset et ne se déclenche jamais. Dans GitPython, le parser de config lit une valeur inerte et la réécrit activée. Dans les trois, <strong>le contrôle existait, écrit avec la même logique que l'erreur qu'il devait attraper</strong>. Si votre validation partage du code avec votre construction, ce n'est pas une validation — c'est la même affirmation énoncée deux fois."
    }
  ]
}
