{
  "title": {
    "es": "Dev News — 9 de septiembre 2026",
    "en": "Dev News — September 9, 2026",
    "fr": "Dev News — 9 septembre 2026"
  },
  "description": {
    "es": "pnpm 12.4 empieza a manejar dependencias de Python y Cargo en el mismo workspace, SvelteKit 3 rompe el contrato de adapters, React Native 0.88 entra en RC y Node 24.21 sube OpenSSL a 3.5.8.",
    "en": "pnpm 12.4 starts managing Python and Cargo dependencies in the same workspace, SvelteKit 3 breaks the adapter contract, React Native 0.88 hits RC, and Node 24.21 moves OpenSSL to 3.5.8.",
    "fr": "pnpm 12.4 gère désormais les dépendances Python et Cargo dans le même workspace, SvelteKit 3 casse le contrat des adapters, React Native 0.88 passe en RC et Node 24.21 monte OpenSSL en 3.5.8."
  },
  "pubDate": "2026-09-09",
  "category": "dev-news",
  "highlights": [
    {
      "text": {
        "es": "pnpm 12.4 instala paquetes de Python y Cargo",
        "en": "pnpm 12.4 installs Python and Cargo packages",
        "fr": "pnpm 12.4 installe des paquets Python et Cargo"
      },
      "anchor": "pnpm-12-4-0",
      "icon": "lucide:package"
    },
    {
      "text": {
        "es": "SvelteKit 3 rompe el contrato de adapters",
        "en": "SvelteKit 3 breaks the adapter contract",
        "fr": "SvelteKit 3 casse le contrat des adapters"
      },
      "anchor": "sveltekit-3-next-27",
      "icon": "simple-icons:svelte"
    },
    {
      "text": {
        "es": "React Native 0.88 entra en RC",
        "en": "React Native 0.88 enters RC",
        "fr": "React Native 0.88 entre en RC"
      },
      "anchor": "rn-0-88-rc",
      "icon": "simple-icons:react"
    },
    {
      "text": {
        "es": "Node 24.21 sube OpenSSL a 3.5.8",
        "en": "Node 24.21 moves OpenSSL to 3.5.8",
        "fr": "Node 24.21 monte OpenSSL en 3.5.8"
      },
      "anchor": "node-24-21-0",
      "icon": "simple-icons:nodedotjs"
    },
    {
      "text": {
        "es": "rspack 2.2.3 suma externals de CSS import",
        "en": "rspack 2.2.3 adds CSS import externals",
        "fr": "rspack 2.2.3 ajoute les externals CSS import"
      },
      "anchor": "rspack-2-2-3",
      "icon": "lucide:package"
    },
    {
      "text": {
        "es": "adapter-bun 1.0 ahora exige Bun 1.4",
        "en": "adapter-bun 1.0 now requires Bun 1.4",
        "fr": "adapter-bun 1.0 exige désormais Bun 1.4"
      },
      "anchor": "sveltekit-adapter-bun",
      "icon": "simple-icons:bun"
    }
  ],
  "tools": [
    {
      "id": "top-stories",
      "name": "Top Stories",
      "theme": "top-stories",
      "icon_emoji": "🔥",
      "anchor": "top-stories",
      "updates": [
        {
          "id": "pnpm-12-4-0",
          "tag": "release",
          "date": "08 Sep 2026",
          "title": {
            "es": "pnpm 12.4.0 maneja npm, Python y Cargo en el mismo workspace",
            "en": "pnpm 12.4.0 manages npm, Python and Cargo in one workspace",
            "fr": "pnpm 12.4.0 gère npm, Python et Cargo dans un même workspace"
          },
          "body_html": {
            "es": "<p>Esto se sale del carril de siempre. Activás <code>python.enabled</code> o <code>cargo.enabled</code> en <code>pnpm-workspace.yaml</code> y un solo <code>pnpm install</code> instala las tres cosas juntas. Los paquetes de Python entran con <code>pnpm add pypi:&lt;package&gt;</code>, apoyándose en <code>pyproject.toml</code>, <code>pylock.toml</code> y un <code>.venv</code> gestionado — con installs frozen y offline, y los ejecutables del entorno disponibles desde <code>pnpm run</code> y <code>pnpm exec</code>. Los crates entran con <code>pnpm add crate:&lt;package&gt;</code>, contra crates.io o registries sparse custom vía <code>cargo.indexUrl</code>, con auth por credenciales de pnpm o <code>CARGO_REGISTRY_TOKEN</code>.</p><p>Suma <code>pnpm pipeline [name]</code>, que instala dependencias frozen y corre tareas de workspace declaradas en <code>pipelines</code>, seleccionando los proyectos afectados. Si tenés un monorepo políglota que hoy pegás con scripts de shell, ojo con esto.</p>",
            "en": "<p>This steps well outside the usual lane. Flip <code>python.enabled</code> or <code>cargo.enabled</code> in <code>pnpm-workspace.yaml</code> and a single <code>pnpm install</code> installs all three together. Python packages come in through <code>pnpm add pypi:&lt;package&gt;</code>, backed by <code>pyproject.toml</code>, <code>pylock.toml</code> and a managed <code>.venv</code> — frozen and offline installs supported, with the environment's executables available from <code>pnpm run</code> and <code>pnpm exec</code>. Crates come in through <code>pnpm add crate:&lt;package&gt;</code>, against crates.io or custom sparse registries via <code>cargo.indexUrl</code>, authenticating with pnpm credentials or <code>CARGO_REGISTRY_TOKEN</code>.</p><p>It also adds <code>pnpm pipeline [name]</code>, which installs frozen dependencies and runs workspace tasks declared under <code>pipelines</code>, selecting affected projects. If you run a polyglot monorepo currently glued together with shell scripts, this one matters.</p>",
            "fr": "<p>Cela sort franchement du cadre habituel. Activez <code>python.enabled</code> ou <code>cargo.enabled</code> dans <code>pnpm-workspace.yaml</code> et un seul <code>pnpm install</code> installe les trois ensemble. Les paquets Python passent par <code>pnpm add pypi:&lt;package&gt;</code>, avec <code>pyproject.toml</code>, <code>pylock.toml</code> et un <code>.venv</code> géré. Les crates passent par <code>pnpm add crate:&lt;package&gt;</code>, vers crates.io ou des registries sparse personnalisés. Ajoute aussi <code>pnpm pipeline [name]</code>.</p>"
          },
          "source": { "url": "https://github.com/pnpm/pnpm/releases/tag/v12.4.0", "label": "github.com/pnpm" }
        },
        {
          "id": "sveltekit-3-next-27",
          "tag": "breaking",
          "date": "08 Sep 2026",
          "title": {
            "es": "SvelteKit 3.0.0-next.27 saca el constructor Server y reemplaza generateManifest",
            "en": "SvelteKit 3.0.0-next.27 drops the Server constructor and replaces generateManifest",
            "fr": "SvelteKit 3.0.0-next.27 retire le constructeur Server et remplace generateManifest"
          },
          "body_html": {
            "es": "<p>Tanda de breaking changes que le pega directo a quien escribe adapters. Se van el constructor <code>Server</code> y <code>SSRManifest</code> de los tipos públicos, y <code>builder.generateManifest</code> queda reemplazado por <code>builder.generateServerInstance</code> más <code>builder.manifest</code>. Además, ahora tira error cuando el servidor no respeta una mutación single-flight pedida por el cliente, y el servidor puede ignorar refreshes de forma explícita.</p><p>Del lado bueno: los adapters pueden recibir la config de Svelte como argumento al agregar plugins de Vite, y pueden overridear <code>getRequest</code> y <code>setResponse</code> durante <code>vite dev</code> y <code>vite preview</code>. Salieron los ocho adapters oficiales alineados el mismo día.</p>",
            "en": "<p>A batch of breaking changes aimed squarely at anyone who writes adapters. The <code>Server</code> constructor and <code>SSRManifest</code> leave the public types, and <code>builder.generateManifest</code> is replaced by <code>builder.generateServerInstance</code> plus <code>builder.manifest</code>. It now also errors when the server doesn't respect a client-requested single-flight mutation, and lets the server explicitly ignore refreshes.</p><p>On the upside: adapters can receive the Svelte config as a function argument when adding Vite plugins, and can override <code>getRequest</code> and <code>setResponse</code> during <code>vite dev</code> and <code>vite preview</code>. All eight official adapters shipped aligned the same day.</p>",
            "fr": "<p>Une salve de breaking changes visant ceux qui écrivent des adapters. Le constructeur <code>Server</code> et <code>SSRManifest</code> quittent les types publics, et <code>builder.generateManifest</code> est remplacé par <code>builder.generateServerInstance</code> et <code>builder.manifest</code>. Une erreur est désormais levée quand le serveur ne respecte pas une mutation single-flight demandée par le client. Les huit adapters officiels sont sortis alignés le même jour.</p>"
          },
          "source": { "url": "https://github.com/sveltejs/kit/releases/tag/%40sveltejs/kit%403.0.0-next.27", "label": "github.com/sveltejs" }
        },
        {
          "id": "rn-0-88-rc",
          "tag": "beta",
          "date": "08 Sep 2026",
          "title": {
            "es": "React Native 0.88.0-rc.0: se va el export Touchable y llega RCTArrayBuffer en iOS",
            "en": "React Native 0.88.0-rc.0: the Touchable export is gone, RCTArrayBuffer lands on iOS",
            "fr": "React Native 0.88.0-rc.0 : l'export Touchable disparaît, RCTArrayBuffer arrive sur iOS"
          },
          "body_html": {
            "es": "<p>Arranca el ciclo de RC de la 0.88. El export raíz <code>Touchable</code> (que nunca estuvo documentado) se elimina — si lo extendías como tipo, pasá a <code>ViewProps</code>. En iOS llega <code>RCTArrayBuffer</code>, la representación ObjC de un <code>ArrayBuffer</code> de JS para TurboModules, con contrato explícito de ownership de bytes: eso es lo que te permite pasar binario entre JS y nativo sin copiar a ciegas. Del lado de C++, <code>&lt;React/FeatureFlags.h&gt;</code> se vuelve el header paraguas y punto de entrada público de <code>react/featureflags</code>.</p>",
            "en": "<p>The 0.88 RC cycle opens. The root <code>Touchable</code> export (never documented) is removed — if you extended it as a type, move to <code>ViewProps</code>. On iOS, <code>RCTArrayBuffer</code> arrives as the ObjC representation of a JS <code>ArrayBuffer</code> for TurboModules, with an explicit byte-ownership contract: that's what lets you pass binary between JS and native without copying blindly. On the C++ side, <code>&lt;React/FeatureFlags.h&gt;</code> becomes the umbrella header and public entry point for <code>react/featureflags</code>.</p>",
            "fr": "<p>Le cycle RC de la 0.88 démarre. L'export racine <code>Touchable</code> (jamais documenté) est supprimé — utilisez <code>ViewProps</code> si vous l'étendiez comme type. Sur iOS, <code>RCTArrayBuffer</code> arrive comme représentation ObjC d'un <code>ArrayBuffer</code> JS pour les TurboModules, avec un contrat explicite de propriété des octets. Côté C++, <code>&lt;React/FeatureFlags.h&gt;</code> devient le header parapluie public de <code>react/featureflags</code>.</p>"
          },
          "source": { "url": "https://github.com/react/react-native/releases/tag/v0.88.0-rc.0", "label": "github.com/react-native" }
        }
      ]
    },
    {
      "id": "js-frameworks",
      "name": "JS Frameworks",
      "theme": "js-frameworks",
      "icon_emoji": "🧩",
      "anchor": "js-frameworks",
      "updates": [
        {
          "id": "sveltekit-adapter-bun",
          "tag": "breaking",
          "date": "08 Sep 2026",
          "title": {
            "es": "SvelteKit adapter-bun 1.0.0-next.2 pasa a exigir Bun 1.4",
            "en": "SvelteKit adapter-bun 1.0.0-next.2 now requires Bun 1.4",
            "fr": "SvelteKit adapter-bun 1.0.0-next.2 exige désormais Bun 1.4"
          },
          "body_html": {
            "es": "<p>El adapter oficial de Bun sube el piso a Bun 1.4, que rutea <code>HEAD</code> a los handlers de <code>GET</code> y hace que <code>stop()</code> resuelva después de un force close — dos comportamientos que el adapter necesitaba y antes tenía que emular. También arregla el build de apps que usan server instrumentation. Que SvelteKit tenga adapter de Bun de primera y le exija una versión concreta dice bastante de dónde está parado Bun hoy.</p>",
            "en": "<p>The official Bun adapter raises its floor to Bun 1.4, which routes <code>HEAD</code> to <code>GET</code> handlers and settles <code>stop()</code> after a force close — two behaviors the adapter needed and previously had to emulate. It also fixes building apps that use server instrumentation. That SvelteKit ships a first-party Bun adapter and pins a concrete version says plenty about where Bun sits now.</p>",
            "fr": "<p>L'adapter Bun officiel relève son socle à Bun 1.4, qui route <code>HEAD</code> vers les handlers <code>GET</code> et fait résoudre <code>stop()</code> après un force close — deux comportements que l'adapter devait auparavant émuler. Corrige aussi le build des apps utilisant server instrumentation.</p>"
          },
          "source": { "url": "https://github.com/sveltejs/kit/releases/tag/%40sveltejs/adapter-bun%401.0.0-next.2", "label": "github.com/sveltejs" }
        },
        {
          "id": "solid-2-rc7",
          "tag": "beta",
          "date": "08 Sep 2026",
          "title": {
            "es": "Solid 2.0.0-rc.7 alinea los overloads de store y suma DEV.attribution.feedback()",
            "en": "Solid 2.0.0-rc.7 aligns store overloads and adds DEV.attribution.feedback()",
            "fr": "Solid 2.0.0-rc.7 aligne les overloads de store et ajoute DEV.attribution.feedback()"
          },
          "body_html": {
            "es": "<p>Los overloads de store quedan alineados entre los entry points de signals, client y server: los stores planos comparten <code>StoreOptions</code>, las formas de projection comparten <code>ProjectionOptions</code>, y los optimistic derivados quedan tipados como refreshables. Lo interesante es <code>DEV.attribution.feedback()</code>: un fold puro sobre los registros que el engine ya guarda, que rankea qué fuentes async retuvieron writes según el tiempo en silencio que pasaste detrás de ellas — <code>holds</code>, <code>heldMs</code>, <code>worstMs</code>, <code>silentMs</code>. Es medir la espera percibida, no la latencia cruda.</p>",
            "en": "<p>Store overloads are aligned across the signals, client and server entry points: plain stores share <code>StoreOptions</code>, projection forms share <code>ProjectionOptions</code>, and derived optimistic stores are typed as refreshable. The interesting bit is <code>DEV.attribution.feedback()</code>: a pure fold over records the engine already keeps, ranking which async sources held writes by the silent time spent behind them — <code>holds</code>, <code>heldMs</code>, <code>worstMs</code>, <code>silentMs</code>. That's measuring perceived waiting, not raw latency.</p>",
            "fr": "<p>Les overloads de store sont alignés entre les entry points signals, client et server. Le point intéressant est <code>DEV.attribution.feedback()</code> : un fold pur sur les enregistrements déjà conservés par le moteur, classant les sources async ayant retenu des writes selon le temps silencieux passé derrière elles.</p>"
          },
          "source": { "url": "https://github.com/solidjs/solid/releases/tag/solid-js%402.0.0-rc.7", "label": "github.com/solidjs" }
        }
      ]
    },
    {
      "id": "build-tools",
      "name": "Build Tools & Runtimes",
      "theme": "build-tools",
      "icon_emoji": "🔧",
      "anchor": "build-tools",
      "updates": [
        {
          "id": "node-24-21-0",
          "tag": "security",
          "date": "08 Sep 2026",
          "title": {
            "es": "Node 24.21.0: OpenSSL 3.5.8, root certs NSS 3.126 y BlockList más rápido",
            "en": "Node 24.21.0: OpenSSL 3.5.8, NSS 3.126 root certs, and a faster BlockList",
            "fr": "Node 24.21.0 : OpenSSL 3.5.8, certificats racine NSS 3.126 et BlockList plus rapide"
          },
          "body_html": {
            "es": "<p>Release con peso en la base criptográfica: OpenSSL sube a 3.5.8, los root certificates se actualizan a NSS 3.126 y Undici pasa a 7.29.1. Como semver-minor llega soporte para cargar claves privadas a través de STORE loaders — útil si tus claves viven en un HSM o en un provider en vez de un archivo. Suma mejoras de performance en <code>net.BlockList</code>, una implementación de histogramas mejorada y hypothesis testing estadístico sobre histogramas en <code>perf_hooks</code>.</p>",
            "en": "<p>A release weighted toward the crypto foundation: OpenSSL moves to 3.5.8, root certificates update to NSS 3.126, and Undici goes to 7.29.1. As a semver-minor, private keys can now be loaded through STORE loaders — useful when your keys live in an HSM or a provider rather than a file. It also brings <code>net.BlockList</code> performance work, an improved histogram implementation, and statistical hypothesis testing over histograms in <code>perf_hooks</code>.</p>",
            "fr": "<p>Une version centrée sur la base cryptographique : OpenSSL passe en 3.5.8, les certificats racine en NSS 3.126, et Undici en 7.29.1. En semver-minor, les clés privées peuvent être chargées via des STORE loaders — utile si vos clés vivent dans un HSM. Ajoute aussi des gains de performance sur <code>net.BlockList</code>.</p>"
          },
          "source": { "url": "https://github.com/nodejs/node/releases/tag/v24.21.0", "label": "github.com/nodejs" }
        },
        {
          "id": "rspack-2-2-3",
          "tag": "release",
          "date": "08 Sep 2026",
          "title": {
            "es": "rspack 2.2.3: externals de CSS import y asset URL, y watcher nativo más confiable",
            "en": "rspack 2.2.3: CSS import and asset URL externals, plus a more reliable native watcher",
            "fr": "rspack 2.2.3 : externals CSS import et asset URL, et watcher natif plus fiable"
          },
          "body_html": {
            "es": "<p>Llegan los external types <code>asset</code>, <code>asset-url</code> y <code>css-import</code>, así el CSS puede referenciar hojas de estilo y assets hosteados en otro lado. Y bien hecho: los <code>@import</code> externos preservan <code>layer</code>, <code>supports</code> y las media conditions, o sea que los estilos condicionales siguen siendo condicionales en el CSS emitido. Con <code>experiments.nativeWatcher</code> activado, <code>watchOptions.ignored</code> ahora acepta una función, y se redujo la pérdida de eventos en ráfagas grandes de cambios en Linux.</p>",
            "en": "<p>The <code>asset</code>, <code>asset-url</code> and <code>css-import</code> external types land, letting CSS reference stylesheets and assets hosted elsewhere. And done right: external <code>@import</code> statements preserve <code>layer</code>, <code>supports</code> and media conditions, so conditional styles stay conditional in the emitted CSS. With <code>experiments.nativeWatcher</code> enabled, <code>watchOptions.ignored</code> now accepts a function, and event loss during large bursts of changes on Linux was reduced.</p>",
            "fr": "<p>Les external types <code>asset</code>, <code>asset-url</code> et <code>css-import</code> arrivent, permettant au CSS de référencer feuilles de style et assets hébergés ailleurs. Les <code>@import</code> externes préservent <code>layer</code>, <code>supports</code> et les media conditions. Avec <code>experiments.nativeWatcher</code>, <code>watchOptions.ignored</code> accepte désormais une fonction.</p>"
          },
          "source": { "url": "https://github.com/web-infra-dev/rspack/releases/tag/v2.2.3", "label": "github.com/rspack" }
        },
        {
          "id": "wrangler-4-130-0",
          "tag": "minor",
          "date": "08 Sep 2026",
          "title": {
            "es": "Wrangler 4.130.0 suma observability por container en wrangler deploy",
            "en": "Wrangler 4.130.0 adds per-container observability to wrangler deploy",
            "fr": "Wrangler 4.130.0 ajoute l'observability par container à wrangler deploy"
          },
          "body_html": {
            "es": "<p><code>containers[].observability</code> permite settings de observabilidad específicos por container, con targeting a nivel aplicación; la <code>observability</code> raíz sigue funcionando como fallback cuando un container no define la suya. <code>wrangler deploy</code> preserva la <code>configuration.observability</code> legacy en apps de containers que todavía usan observabilidad basada en rollout. Además, los diffs de aplicaciones existentes se normalizan aunque los resource limits guardados no mapeen a un instance type con nombre — así que metadata solo-API y nombres de imagen equivalentes dejan de aparecer como ediciones y de forzar rollouts.</p>",
            "en": "<p><code>containers[].observability</code> allows container-specific observability settings with application-level targeting; root <code>observability</code> still works as a fallback when a container defines none. <code>wrangler deploy</code> preserves legacy <code>configuration.observability</code> for container apps still on rollout-based observability. Existing application diffs are also normalized even when stored resource limits can't map to a named instance type — so API-only metadata and equivalent managed-registry image names stop showing up as edits and stop forcing rollouts.</p>",
            "fr": "<p><code>containers[].observability</code> autorise des réglages d'observability par container avec ciblage au niveau application ; l'<code>observability</code> racine reste un fallback. Les diffs d'applications existantes sont normalisés même quand les resource limits stockés ne correspondent à aucun instance type nommé.</p>"
          },
          "source": { "url": "https://github.com/cloudflare/workers-sdk/releases/tag/wrangler%404.130.0", "label": "github.com/cloudflare" }
        },
        {
          "id": "electron-44-3-0",
          "tag": "release",
          "date": "08 Sep 2026",
          "title": {
            "es": "Electron 44.3.0 suma ELECTRON_DEBUG_DRAGGABLE_REGIONS y lleva el scoping de File System Access a estable",
            "en": "Electron 44.3.0 adds ELECTRON_DEBUG_DRAGGABLE_REGIONS and brings File System Access scoping to stable",
            "fr": "Electron 44.3.0 ajoute ELECTRON_DEBUG_DRAGGABLE_REGIONS et amène le scoping File System Access en stable"
          },
          "body_html": {
            "es": "<p>La variable de entorno <code>ELECTRON_DEBUG_DRAGGABLE_REGIONS</code> visualiza y loguea las draggable regions en apps sin empaquetar — exactamente lo que necesitabas para debuggear title bars custom sin adivinar dónde termina el área arrastrable. Y el cambio de permisos de File System Access que vimos ayer en la alpha de la 45 aterriza acá en estable, backporteado también a 42 y 43: los pedidos y el evento <code>file-system-access-restricted</code> quedan escopeados al documento que los pide. Suma el fix de Ctrl+Y que no hacía yank del texto matado con Ctrl+K en campos de texto en macOS.</p>",
            "en": "<p>The <code>ELECTRON_DEBUG_DRAGGABLE_REGIONS</code> environment variable visualizes and logs draggable regions in unpackaged apps — exactly what you needed to debug custom title bars without guessing where the draggable area ends. And the File System Access permission change seen yesterday in the 45 alpha lands here on stable, backported to 42 and 43 as well: requests and the <code>file-system-access-restricted</code> event are scoped to the requesting document. Also fixes Ctrl+Y not yanking text killed with Ctrl+K in macOS text fields.</p>",
            "fr": "<p>La variable d'environnement <code>ELECTRON_DEBUG_DRAGGABLE_REGIONS</code> visualise et journalise les draggable regions dans les apps non packagées — utile pour déboguer les title bars personnalisées. Le changement de permissions File System Access vu hier en alpha 45 arrive ici en stable, rétroporté sur 42 et 43.</p>"
          },
          "source": { "url": "https://github.com/electron/electron/releases/tag/v44.3.0", "label": "github.com/electron" }
        }
      ]
    }
  ],
  "notable_trends": [
    {
      "es": "pnpm dejó de ser un package manager de npm y pasó a ser el package manager del monorepo: Python y Cargo en el mismo install. Es la apuesta más ambiciosa del ecosistema JS en meses, y va a decidir muchas migraciones.",
      "en": "pnpm stopped being an npm package manager and became the monorepo's package manager: Python and Cargo in the same install. It's the most ambitious bet in the JS ecosystem in months, and it will decide a lot of migrations.",
      "fr": "pnpm a cessé d'être un gestionnaire de paquets npm pour devenir le gestionnaire du monorepo : Python et Cargo dans le même install. Le pari le plus ambitieux de l'écosystème JS depuis des mois."
    },
    {
      "es": "Bun se está volviendo target de primera para los frameworks, no una alternativa. SvelteKit publica adapter-bun oficial y le fija piso en Bun 1.4 por comportamientos concretos del runtime. Eso es dependencia real, no compatibilidad de cortesía.",
      "en": "Bun is becoming a first-class framework target, not an alternative. SvelteKit ships an official adapter-bun and pins Bun 1.4 for concrete runtime behaviors. That's real dependency, not courtesy compatibility.",
      "fr": "Bun devient une cible de premier plan pour les frameworks, pas une alternative. SvelteKit publie un adapter-bun officiel et fixe Bun 1.4 pour des comportements runtime concrets."
    },
    {
      "es": "Día de contratos que se rompen a propósito: SvelteKit saca el constructor Server, React Native elimina el export Touchable, Solid retipea sus stores. Los tres van hacia superficies públicas más chicas y explícitas. Duele en la migración, se agradece después.",
      "en": "A day of contracts broken on purpose: SvelteKit drops the Server constructor, React Native removes the Touchable export, Solid retypes its stores. All three move toward smaller, more explicit public surfaces. It hurts during migration and pays off after.",
      "fr": "Une journée de contrats brisés volontairement : SvelteKit retire le constructeur Server, React Native supprime l'export Touchable, Solid retype ses stores. Les trois vont vers des surfaces publiques plus petites et explicites."
    }
  ]
}
