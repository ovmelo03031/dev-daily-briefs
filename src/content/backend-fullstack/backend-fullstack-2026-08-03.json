{
  "title": {
    "es": "Backend & Fullstack — 3 ago 2026",
    "en": "Backend & Fullstack — Aug 3, 2026",
    "fr": "Backend & Fullstack — 3 août 2026"
  },
  "description": {
    "es": "Node.js suelta 26.6.0 y 24.19.0 LTS el mismo día, Cloudflare abre gRPC y TCP entrante en Workers, GitHub lanza stacked PRs en preview y retira GitHub Models.",
    "en": "Node.js ships 26.6.0 and 24.19.0 LTS on the same day, Cloudflare opens gRPC and inbound TCP on Workers, GitHub launches stacked PRs in preview and retires GitHub Models.",
    "fr": "Node.js sort 26.6.0 et 24.19.0 LTS le même jour, Cloudflare ouvre gRPC et TCP entrant sur Workers, GitHub lance les stacked PRs en preview et retire GitHub Models."
  },
  "pubDate": "2026-08-03",
  "category": "backend-fullstack",
  "highlights": [
    {
      "text": {
        "es": "Node 24.19 LTS y 26.6 el mismo día",
        "en": "Node 24.19 LTS and 26.6 land together",
        "fr": "Node 24.19 LTS et 26.6 le même jour"
      },
      "anchor": "node-aug3-releases",
      "icon": "simple-icons:nodedotjs"
    },
    {
      "text": {
        "es": "gRPC y TCP entrante en Workers",
        "en": "gRPC and inbound TCP on Workers",
        "fr": "gRPC et TCP entrant sur Workers"
      },
      "anchor": "cf-grpc-workers",
      "icon": "simple-icons:cloudflare"
    },
    {
      "text": {
        "es": "Stacked PRs, nativos en GitHub",
        "en": "Stacked PRs, native in GitHub",
        "fr": "Stacked PRs, natifs dans GitHub"
      },
      "anchor": "gh-stacked-prs",
      "icon": "simple-icons:github"
    },
    {
      "text": {
        "es": "Agentes sin container por defecto",
        "en": "Agent runtime without a container",
        "fr": "Runtime d agents sans container"
      },
      "anchor": "cf-computer",
      "icon": "lucide:layers"
    },
    {
      "text": {
        "es": "ML-DSA de punta a punta hasta tu origin",
        "en": "ML-DSA all the way to your origin",
        "fr": "ML-DSA de bout en bout vers votre origin"
      },
      "anchor": "cf-pq-origins",
      "icon": "lucide:shield"
    },
    {
      "text": {
        "es": "GitHub Models se retira: migrá ya",
        "en": "GitHub Models retired: migrate now",
        "fr": "GitHub Models retiré : migrez"
      },
      "anchor": "gh-models-retired",
      "icon": "lucide:package"
    }
  ],
  "tools": [
    {
      "id": "top-stories",
      "name": "Top Stories",
      "theme": "top-stories",
      "icon_emoji": "🔥",
      "anchor": "node-aug3-releases",
      "updates": [
        {
          "id": "node-aug3-releases",
          "tag": "release",
          "date": "03 Aug 2026",
          "title": {
            "es": "Node.js 24.19.0 LTS: streams, TLS y keep-alive con más control",
            "en": "Node.js 24.19.0 LTS: more control over streams, TLS and keep-alive",
            "fr": "Node.js 24.19.0 LTS : plus de contrôle sur streams, TLS et keep-alive"
          },
          "body_html": {
            "es": "<p>Cinco días después del release de seguridad conjunta, la línea LTS suma features de verdad. Lo grueso: <code>blob.textStream()</code>, <code>ReadableStreamTee</code> expuesto públicamente, y <code>fs.readFile()</code> aceptando buffers provistos por vos — chau una alocación por lectura en paths calientes. En red aparecen <code>TCP_KEEPINTVL</code> y <code>TCP_KEEPCNT</code> en <code>net.setKeepAlive()</code>, más <code>Socket.bindSync()</code> y <code>Socket.connectSync()</code> en dgram. En TLS entran el reporte del grupo negociado y la opción <code>certificateCompression</code>; Argon2 y encap/decap pasan a stable. Y para ESM: <code>--experimental-import-text</code> para importar archivos de texto como string. <code>stream.compose</code> queda stable. Si estabas esperando para saltar de 22.x, este es el momento.</p>",
            "en": "<p>Five days after the joint security release, the LTS line gets real features. The meat: <code>blob.textStream()</code>, a publicly exposed <code>ReadableStreamTee</code>, and <code>fs.readFile()</code> accepting caller-supplied buffers — one allocation less per read on hot paths. Networking gains <code>TCP_KEEPINTVL</code> and <code>TCP_KEEPCNT</code> in <code>net.setKeepAlive()</code>, plus <code>Socket.bindSync()</code> and <code>Socket.connectSync()</code> for dgram. TLS adds negotiated-group reporting and a <code>certificateCompression</code> option; Argon2 and encap/decap are now stable. For ESM, <code>--experimental-import-text</code> imports text files as strings. <code>stream.compose</code> is marked stable. If you were holding off on moving from 22.x, this is the release to plan against.</p>",
            "fr": "<p>Cinq jours après la release de sécurité conjointe, la ligne LTS gagne de vraies features. L essentiel : <code>blob.textStream()</code>, <code>ReadableStreamTee</code> exposé publiquement, et <code>fs.readFile()</code> qui accepte des buffers fournis par l appelant — une allocation de moins par lecture sur les chemins chauds. Côté réseau : <code>TCP_KEEPINTVL</code> et <code>TCP_KEEPCNT</code> dans <code>net.setKeepAlive()</code>, plus <code>Socket.bindSync()</code> et <code>Socket.connectSync()</code> pour dgram. TLS ajoute le report du groupe négocié et l option <code>certificateCompression</code> ; Argon2 et encap/decap passent stable. Pour ESM, <code>--experimental-import-text</code> importe des fichiers texte en string. <code>stream.compose</code> devient stable.</p>"
          },
          "source": {
            "url": "https://nodejs.org/en/blog/release/v24.19.0",
            "label": "nodejs.org"
          }
        },
        {
          "id": "cf-grpc-workers",
          "tag": "beta",
          "date": "03 Aug 2026",
          "title": {
            "es": "Workers y Containers aceptan TCP entrante y gRPC full-duplex",
            "en": "Workers and Containers accept inbound TCP and full-duplex gRPC",
            "fr": "Workers et Containers acceptent le TCP entrant et gRPC full-duplex"
          },
          "body_html": {
            "es": "<p>Tres cosas de una: un handler <code>connect(socket)</code> que recibe sockets TCP entrantes desde Spectrum y los reenvía a Durable Objects o Containers; servidores gRPC en cualquier lenguaje corriendo en Cloudflare Containers con streaming bidireccional; y Workers actuando como servidor o cliente gRPC vía <code>@connectrpc/connect</code>, con traducción automática entre gRPC y gRPC-web. Traducido: podés poner un Worker adelante de tu backend gRPC existente sin tocar los clientes. Está en private beta con signup abierto, pero el edge dejando de ser solo HTTP es un cambio de categoría.</p>",
            "en": "<p>Three things at once: a <code>connect(socket)</code> handler that accepts inbound TCP sockets from Spectrum and forwards them to Durable Objects or Containers; gRPC servers in any language running on Cloudflare Containers with bidirectional streaming; and Workers acting as gRPC server or client via <code>@connectrpc/connect</code>, with automatic translation between gRPC and gRPC-web. Practically: you can put a Worker in front of an existing gRPC backend without touching the clients. Private beta with open signup — but the edge no longer being HTTP-only is a category change.</p>",
            "fr": "<p>Trois choses d un coup : un handler <code>connect(socket)</code> qui accepte les sockets TCP entrants depuis Spectrum et les redirige vers des Durable Objects ou Containers ; des serveurs gRPC dans n importe quel langage sur Cloudflare Containers avec streaming bidirectionnel ; et des Workers comme serveur ou client gRPC via <code>@connectrpc/connect</code>, avec traduction automatique entre gRPC et gRPC-web. En pratique : vous pouvez placer un Worker devant un backend gRPC existant sans toucher aux clients. Private beta avec inscription ouverte.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/grpc-workers/",
            "label": "blog.cloudflare.com"
          }
        },
        {
          "id": "gh-stacked-prs",
          "tag": "beta",
          "date": "30 Jul 2026",
          "title": {
            "es": "Stacked pull requests, nativos en GitHub y en public preview",
            "en": "Stacked pull requests go native on GitHub, in public preview",
            "fr": "Stacked pull requests natifs sur GitHub, en public preview"
          },
          "body_html": {
            "es": "<p>Por fin: partir un cambio grande en PRs chicos y ordenados, cada uno apuntando al de abajo, sin scripts caseros ni <code>git-town</code>. Hay un stack map para ver cómo encajan las capas, review en paralelo por capa, y merge de todo el stack o capa por capa con rebase automático de las PRs que quedan abiertas. Branch protections, required checks y merge queues siguen funcionando igual. Se usa desde github.com, la app móvil, agentes, o CLI con <code>gh extension install github/gh-stack</code>. Si tu equipo abre PRs de 800 líneas porque partirlas era un dolor, se te acabó la excusa.</p>",
            "en": "<p>Finally: split a large change into small, ordered PRs, each targeting the one below, without homegrown scripts or <code>git-town</code>. There is a stack map showing how layers fit, parallel per-layer review, and merging the whole stack or one layer at a time with automatic rebasing of the remaining open PRs. Branch protections, required checks and merge queues keep working. Drive it from github.com, mobile, coding agents, or the CLI via <code>gh extension install github/gh-stack</code>. If your team ships 800-line PRs because splitting them was painful, that excuse is gone.</p>",
            "fr": "<p>Enfin : découper un gros changement en PRs petites et ordonnées, chacune ciblant celle du dessous, sans scripts maison ni <code>git-town</code>. Une stack map montre comment les couches s emboîtent, la review se fait en parallèle par couche, et le merge peut porter sur tout le stack ou couche par couche avec rebase automatique des PRs restantes. Branch protections, required checks et merge queues continuent de fonctionner. Utilisable depuis github.com, mobile, les agents, ou la CLI via <code>gh extension install github/gh-stack</code>.</p>"
          },
          "source": {
            "url": "https://github.blog/changelog/2026-07-30-stacked-pull-requests-are-now-in-public-preview/",
            "label": "github.blog"
          }
        }
      ]
    },
    {
      "id": "runtimes",
      "name": "Backend TypeScript & Runtimes",
      "theme": "runtimes",
      "icon_emoji": "⚡",
      "anchor": "node-26-6-0",
      "updates": [
        {
          "id": "node-26-6-0",
          "tag": "release",
          "date": "03 Aug 2026",
          "title": {
            "es": "Node.js 26.6.0: FFI con event loop y logs en el test runner",
            "en": "Node.js 26.6.0: FFI event loop access and test runner logging",
            "fr": "Node.js 26.6.0 : accès à l event loop en FFI et logs dans le test runner"
          },
          "body_html": {
            "es": "<p>La línea Current suma tres SEMVER-MINOR que interesan si escribís tooling. <code>getCurrentEventLoop()</code> expone el event loop actual a operaciones FFI — clave para bindings nativos que necesitan agendar trabajo sin pelearse con el thread pool. En el test runner llegan <code>context.log()</code> y el evento <code>test:log</code>, más <code>entryFile</code> en los eventos de <code>TestStream</code>: por fin se puede correlacionar salida de test con el archivo que la generó desde un reporter propio. Además, optimizaciones en <code>once()</code> y <code>removeListener()</code>, compresión de certificados TLS en QUIC, y npm sube a 11.18.0.</p>",
            "en": "<p>The Current line adds three SEMVER-MINOR changes that matter if you write tooling. <code>getCurrentEventLoop()</code> exposes the current event loop to FFI operations — key for native bindings that need to schedule work without fighting the thread pool. The test runner gains <code>context.log()</code> and a <code>test:log</code> event, plus <code>entryFile</code> in <code>TestStream</code> events: you can finally correlate test output with the file that produced it from a custom reporter. Also: <code>once()</code> and <code>removeListener()</code> optimizations, TLS certificate compression in QUIC, and npm bumped to 11.18.0.</p>",
            "fr": "<p>La ligne Current ajoute trois changements SEMVER-MINOR utiles si vous écrivez du tooling. <code>getCurrentEventLoop()</code> expose l event loop courant aux opérations FFI — essentiel pour les bindings natifs qui doivent planifier du travail sans se battre avec le thread pool. Le test runner gagne <code>context.log()</code> et l événement <code>test:log</code>, plus <code>entryFile</code> dans les événements <code>TestStream</code>. Également : optimisations de <code>once()</code> et <code>removeListener()</code>, compression de certificats TLS en QUIC, et npm passe à 11.18.0.</p>"
          },
          "source": {
            "url": "https://nodejs.org/en/blog/release/v26.6.0",
            "label": "nodejs.org"
          }
        }
      ]
    },
    {
      "id": "architecture",
      "name": "Architecture & Best Practices",
      "theme": "architecture",
      "icon_emoji": "🏗️",
      "anchor": "cf-computer",
      "updates": [
        {
          "id": "cf-computer",
          "tag": "release",
          "date": "03 Aug 2026",
          "title": {
            "es": "@cloudflare/computer: runtime de agentes que elige isolate o container solo",
            "en": "@cloudflare/computer: an agent runtime that picks isolate or container for you",
            "fr": "@cloudflare/computer : un runtime d agents qui choisit isolate ou container"
          },
          "body_html": {
            "es": "<p>El argumento arquitectónico es más interesante que el producto: no hay compute en el mundo para darle a cada agente de cada usuario su propio container. La respuesta de Cloudflare es un runtime open source con un filesystem virtual respaldado por SQLite, accesible desde varios entornos de ejecución. Los comandos de shell se traducen a JavaScript vía <code>just-bash</code> y corren en dynamic workers; solo cuando hace falta Linux de verdad se levanta un Container con el filesystem montado por FUSE. El objetivo declarado: mantener los containers por debajo del 10% de la carga de agentes. Traducido a decisión de diseño — el sandbox por defecto debería ser el más barato que resuelva el caso, no el más capaz.</p>",
            "en": "<p>The architectural argument is more interesting than the product: there is not enough compute on Earth to give every user of every company their own containerized agent environment. Cloudflare answers with an open-source runtime built on a SQLite-backed virtual filesystem reachable from several execution environments. Shell commands are translated into JavaScript via <code>just-bash</code> and run in dynamic workers; a Container with a FUSE-mounted filesystem spins up only when real Linux tooling is required. The stated target: keep containers under 10% of agent workloads. As a design rule — your default sandbox should be the cheapest one that solves the case, not the most capable one.</p>",
            "fr": "<p>L argument architectural est plus intéressant que le produit : il n y a pas assez de compute pour donner à chaque agent de chaque utilisateur son propre container. Cloudflare répond avec un runtime open source bâti sur un filesystem virtuel adossé à SQLite, accessible depuis plusieurs environnements d exécution. Les commandes shell sont traduites en JavaScript via <code>just-bash</code> et tournent dans des dynamic workers ; un Container avec filesystem monté en FUSE ne démarre que si du vrai outillage Linux est nécessaire. Objectif annoncé : garder les containers sous 10% des charges d agents.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/cloudflare-computer/",
            "label": "blog.cloudflare.com"
          }
        },
        {
          "id": "cf-agents-week",
          "tag": "notable",
          "date": "02 Aug 2026",
          "title": {
            "es": "Agents Week: la cloud se diseñó para humanos, no para agentes",
            "en": "Agents Week: the cloud was designed for humans, not agents",
            "fr": "Agents Week : le cloud a été conçu pour des humains, pas des agents"
          },
          "body_html": {
            "es": "<p>Cloudflare abrió su segunda Agents Week con una premisa que vale más que el anuncio: la cloud y la web actuales se construyeron para la atención y las decisiones humanas. Cinco ejes durante la semana — primitivas y capa de ejecución, ciclo de vida de desarrollo agéntico, acceso organizacional seguro, una web nativa para agentes (descubrimiento, acceso y pagos), e interacción humano-agente. Si estás diseñando servicios que van a ser consumidos por agentes y no por browsers, esta es la conversación de arquitectura del año: la superficie de tu API deja de optimizarse para pantallas y pasa a optimizarse para velocidad, estructura y acceso directo.</p>",
            "en": "<p>Cloudflare opened its second Agents Week with a premise worth more than the announcements: today's cloud and web were built for human attention and human decision-making. Five threads across the week — primitives and the execution layer, the agentic software development lifecycle, secure organizational access, an agent-native web (discovery, access, payments), and human-agent interaction. If you are designing services that will be consumed by agents rather than browsers, this is the architecture conversation of the year: your API surface stops optimizing for screens and starts optimizing for speed, structure and direct access.</p>",
            "fr": "<p>Cloudflare a ouvert sa deuxième Agents Week avec une prémisse qui vaut plus que les annonces : le cloud et le web actuels ont été bâtis pour l attention et les décisions humaines. Cinq axes sur la semaine — primitives et couche d exécution, cycle de vie du développement agentique, accès organisationnel sécurisé, un web natif pour agents (découverte, accès, paiements), et interaction humain-agent. Si vous concevez des services consommés par des agents plutôt que des navigateurs, c est la conversation d architecture de l année.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/agents-week-welcome/",
            "label": "blog.cloudflare.com"
          }
        }
      ]
    },
    {
      "id": "cloud",
      "name": "Cloud & DevOps",
      "theme": "cloud",
      "icon_emoji": "☁️",
      "anchor": "cf-billable-usage",
      "updates": [
        {
          "id": "cf-billable-usage",
          "tag": "release",
          "date": "03 Aug 2026",
          "title": {
            "es": "Billable Usage API: costos de Cloudflare por endpoint, en formato FOCUS",
            "en": "Billable Usage API: Cloudflare costs from one endpoint, in FOCUS format",
            "fr": "Billable Usage API : les coûts Cloudflare via un endpoint, au format FOCUS"
          },
          "body_html": {
            "es": "<p>Un solo endpoint devuelve uso y costo diario desglosado por producto para cuentas self-serve — Workers, R2, D1 y el resto — con campos <code>ServiceName</code>, <code>ChargePeriodStart/End</code>, <code>ConsumedUnit</code> y <code>ContractedCost</code>. Lo importante no es el endpoint sino que sigue el estándar <strong>FOCUS</strong> (FinOps Open Cost and Usage Specification), así que entra directo en tu toolchain de FinOps sin parsers a medida. Hay integración con Vantage para ver el gasto de Cloudflare al lado del de AWS o Azure. En el roadmap: ventanas de tiempo más finas y forecast de gasto.</p>",
            "en": "<p>A single endpoint returns daily usage and cost broken down by product for self-serve accounts — Workers, R2, D1 and the rest — with <code>ServiceName</code>, <code>ChargePeriodStart/End</code>, <code>ConsumedUnit</code> and <code>ContractedCost</code> fields. The point is not the endpoint but that it follows the <strong>FOCUS</strong> standard (FinOps Open Cost and Usage Specification), so it drops into an existing FinOps toolchain without bespoke parsers. A Vantage integration puts Cloudflare spend next to AWS or Azure. On the roadmap: finer time windows and spend forecasting.</p>",
            "fr": "<p>Un seul endpoint renvoie l usage et le coût quotidiens ventilés par produit pour les comptes self-serve — Workers, R2, D1 et le reste — avec les champs <code>ServiceName</code>, <code>ChargePeriodStart/End</code>, <code>ConsumedUnit</code> et <code>ContractedCost</code>. L essentiel n est pas l endpoint mais le respect du standard <strong>FOCUS</strong> (FinOps Open Cost and Usage Specification), qui permet de l intégrer sans parsers sur mesure. Une intégration Vantage place les dépenses Cloudflare à côté d AWS ou Azure.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/billable-usage-api/",
            "label": "blog.cloudflare.com"
          }
        },
        {
          "id": "gh-models-retired",
          "tag": "deprecation",
          "date": "30 Jul 2026",
          "title": {
            "es": "GitHub Models se retira por completo: playground, catálogo, inference API y BYOK",
            "en": "GitHub Models fully retired: playground, catalog, inference API and BYOK",
            "fr": "GitHub Models entièrement retiré : playground, catalogue, inference API et BYOK"
          },
          "body_html": {
            "es": "<p>No es deprecation gradual: playground, catálogo de modelos, inference API y bring-your-own-key dejan de estar disponibles para todos los clientes, incluidos los que tenían uso activo. Si tenías un prototipo o un job de CI pegado a la inference API, ya se rompió. Los caminos de migración que propone GitHub son Microsoft Foundry para el catálogo de modelos, o GitHub Copilot si lo que querías era armar workflows de IA dentro de GitHub. Moraleja de arquitectura: una capa de abstracción propia sobre el proveedor de inferencia no es over-engineering, es la diferencia entre cambiar un adapter y reescribir un servicio.</p>",
            "en": "<p>This is not a gradual deprecation: playground, model catalog, inference API and bring-your-own-key are gone for every customer, including those with active usage. If you had a prototype or a CI job wired to the inference API, it is already broken. GitHub's migration paths are Microsoft Foundry for the model catalog, or GitHub Copilot if you wanted to build AI workflows inside GitHub. The architecture lesson: your own abstraction layer over an inference provider is not over-engineering — it is the difference between swapping an adapter and rewriting a service.</p>",
            "fr": "<p>Ce n est pas une dépréciation progressive : playground, catalogue de modèles, inference API et bring-your-own-key disparaissent pour tous les clients, y compris ceux avec un usage actif. Si un prototype ou un job de CI était branché sur l inference API, il est déjà cassé. Les chemins de migration proposés sont Microsoft Foundry pour le catalogue, ou GitHub Copilot pour les workflows IA dans GitHub. Leçon d architecture : une couche d abstraction maison au-dessus du fournisseur d inférence n est pas de l over-engineering.</p>"
          },
          "source": {
            "url": "https://github.blog/changelog/2026-07-30-github-models-is-now-retired/",
            "label": "github.blog"
          }
        }
      ]
    },
    {
      "id": "fullstack",
      "name": "Fullstack",
      "theme": "fullstack",
      "icon_emoji": "🔗",
      "anchor": "cf-python-js-rpc",
      "updates": [
        {
          "id": "cf-python-js-rpc",
          "tag": "release",
          "date": "03 Aug 2026",
          "title": {
            "es": "Workers RPC cruza Python y JavaScript sin schemas ni serialización",
            "en": "Workers RPC now crosses Python and JavaScript with no schemas or serialization",
            "fr": "Workers RPC traverse Python et JavaScript sans schémas ni sérialisation"
          },
          "body_html": {
            "es": "<p>Llamás un método definido en un Worker de Python desde uno de JavaScript, y al revés, sin API intermedia, sin schema y sin código de serialización. La traducción de tipos la hace el FFI de Pyodide: enteros, floats, booleanos, dicts y listas mapean directo. Para objetos propios de la plataforma como <code>Request</code> y <code>Response</code> hay una capa de conversión en el paquete <code>workers-runtime-sdk</code>, así que del lado Python trabajás con objetos idiomáticos y no con proxies de JavaScript. Las llamadas se comportan como funciones normales — promise en JS, future en Python — y las excepciones propagan. Config: un service binding y listo. La implementación es open source en <code>workerd</code>.</p>",
            "en": "<p>Call a method defined in a Python Worker from a JavaScript one, and the other way round, with no intermediate API, no schema and no serialization code. Type translation is handled by Pyodide's FFI: integers, floats, booleans, dicts and lists map directly. For platform objects like <code>Request</code> and <code>Response</code>, a conversion layer in the <code>workers-runtime-sdk</code> package means the Python side works with idiomatic objects instead of JavaScript proxies. Calls behave like ordinary functions — a promise in JS, a future in Python — and exceptions propagate. Configuration is a service binding. The implementation is open source in <code>workerd</code>.</p>",
            "fr": "<p>Appelez une méthode définie dans un Worker Python depuis un Worker JavaScript, et inversement, sans API intermédiaire, sans schéma et sans code de sérialisation. La traduction de types est assurée par le FFI de Pyodide : entiers, floats, booléens, dicts et listes se mappent directement. Pour les objets de plateforme comme <code>Request</code> et <code>Response</code>, une couche de conversion dans <code>workers-runtime-sdk</code> permet de manipuler des objets idiomatiques côté Python. Les appels se comportent comme des fonctions normales — promise en JS, future en Python — et les exceptions se propagent.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/python-workers-rpc/",
            "label": "blog.cloudflare.com"
          }
        }
      ]
    },
    {
      "id": "security",
      "name": "Security",
      "theme": "security",
      "icon_emoji": "🔒",
      "anchor": "cf-pq-origins",
      "updates": [
        {
          "id": "cf-pq-origins",
          "tag": "notable",
          "date": "29 Jul 2026",
          "title": {
            "es": "Autenticación post-cuántica hasta el origin: ML-DSA en COTS y mTLS",
            "en": "Post-quantum authentication to origins: ML-DSA in COTS and mTLS",
            "fr": "Authentification post-quantique vers l origin : ML-DSA dans COTS et mTLS"
          },
          "body_html": {
            "es": "<p>El tramo entre Cloudflare y tu origin ya puede autenticarse con <strong>ML-DSA</strong> (FIPS 204), en los tres parameter sets: ML-DSA-44, 65 y 87 — con el 44 recomendado por performance y una categoría NIST 2 cómoda. Aplica a Custom Origin Trust Store y a Authenticated Origin Pulls. Del lado tuyo hay trabajo real: generar cadenas ML-DSA con OpenSSL 3.5.0+, subirlas en encoding seed-only de FIPS 204, configurar NGINX para presentar o verificar esos certificados, poner SSL/TLS en Full (strict) y — esto es lo que se olvida — deshabilitar los mecanismos de autenticación vulnerables a cuántica para que no te bajen por downgrade. Primer hito de un roadmap que apunta a 2029.</p>",
            "en": "<p>The hop between Cloudflare and your origin can now authenticate with <strong>ML-DSA</strong> (FIPS 204) across all three parameter sets: ML-DSA-44, 65 and 87 — with 44 recommended for performance at a comfortable NIST category 2. It applies to Custom Origin Trust Store and Authenticated Origin Pulls. There is real work on your side: generate ML-DSA chains with OpenSSL 3.5.0+, upload them in FIPS 204 seed-only encoding, configure NGINX to present or verify those certificates, set SSL/TLS to Full (strict), and — the step people forget — disable quantum-vulnerable authentication mechanisms so you cannot be downgraded. First milestone of a roadmap targeting 2029.</p>",
            "fr": "<p>Le saut entre Cloudflare et votre origin peut désormais s authentifier avec <strong>ML-DSA</strong> (FIPS 204), sur les trois parameter sets : ML-DSA-44, 65 et 87 — le 44 étant recommandé pour la performance avec une catégorie NIST 2 confortable. Cela concerne Custom Origin Trust Store et Authenticated Origin Pulls. Le travail côté vous est réel : générer les chaînes ML-DSA avec OpenSSL 3.5.0+, les téléverser en encodage seed-only FIPS 204, configurer NGINX, passer SSL/TLS en Full (strict), et surtout désactiver les mécanismes d authentification vulnérables au quantique pour éviter les downgrades.</p>"
          },
          "source": {
            "url": "https://blog.cloudflare.com/post-quantum-authentication-to-origins/",
            "label": "blog.cloudflare.com"
          }
        }
      ]
    }
  ],
  "notable_trends": [
    {
      "es": "El edge dejó de ser solo HTTP. gRPC full-duplex, TCP entrante y RPC cruzando lenguajes en el mismo runtime: lo que antes exigía un VPS ahora entra en un Worker.",
      "en": "The edge is no longer HTTP-only. Full-duplex gRPC, inbound TCP and cross-language RPC in the same runtime: what used to need a VPS now fits in a Worker.",
      "fr": "L edge n est plus seulement HTTP. gRPC full-duplex, TCP entrant et RPC inter-langages dans le même runtime : ce qui exigeait un VPS tient désormais dans un Worker."
    },
    {
      "es": "El isolate vuelve a ganarle al container como default. El argumento ya no es solo cold start: es que no hay compute en el planeta para darle un container a cada agente.",
      "en": "The isolate is beating the container as the default again. The argument is no longer just cold start: there is not enough compute on the planet to give every agent its own container.",
      "fr": "L isolate redevient le défaut face au container. L argument n est plus seulement le cold start : il n y a pas assez de compute pour donner un container à chaque agent."
    },
    {
      "es": "Post-cuántico pasó de handshake a autenticación. Después de la key exchange, ahora toca ML-DSA en los certificados — y eso implica tocar tu NGINX, no solo esperar al proveedor.",
      "en": "Post-quantum moved from handshake to authentication. After key exchange comes ML-DSA in certificates — and that means touching your own NGINX, not just waiting on a provider.",
      "fr": "Le post-quantique passe du handshake à l authentification. Après l échange de clés vient ML-DSA dans les certificats — ce qui implique de toucher à votre NGINX, pas seulement d attendre un fournisseur."
    }
  ]
}
